doctype html
html
  head
    title Visual
    meta(charset='utf-8')
    meta(name='viewport', content='initial-scale=1.0, user-scalable=no')
    style(type='text/css').
      html, body, #root{
      width: 100%;
      height: 100%;
      padding: 0;
      margin: 0;
      overflow: hidden;
      background-color: #FFF;
      }
    script(type='text/javascript', src='https://api.map.baidu.com/api?v=2.0&ak=wrBbPYKj9EwzBGSPIqmWOGgF5uhcwxXn')
  body
    #root
    script.
      window.algos = (!{JSON.stringify(algos)}).map(v=>v.name);
    script(type='text/javascript', src='main.js')
    script.
      let userid = !{JSON.stringify(usid)};
      alert(userid);
      if(!userid){
        document.querySelector('#info').style.display = "none";
      }else{
        document.querySelector('#info>button').innerText = "退出:"+userid;
        document.querySelector('#info>button').onclick = function(){
          location.href = "/users/logout";
        };
      }